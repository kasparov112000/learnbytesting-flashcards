/**
 * FSRS stability threshold for "mastered" status.
 * A card is considered mastered when its FSRS stability exceeds this value (in days).
 * Used consistently across flashcards microservice and guided-lessons microservice.
 */
export const MASTERY_STABILITY_THRESHOLD = 21;
